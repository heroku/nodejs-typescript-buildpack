#!/usr/bin/env bash

set -e
set -o pipefail

layers_dir=$1

bp_dir=$(cd "$(dirname "$0")"/..; pwd)
build_dir=$(pwd)

# shellcheck source=/dev/null
source "$bp_dir/lib/build.sh"

if check_tsc_binary "$build_dir" ; then
  export PATH=./node_modules/typescript/bin:$PATH
fi

build_ts_app "$build_dir"
